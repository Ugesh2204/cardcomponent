import React from 'react';
import './Button.css';

const Button = ({
    btnColor,
    btntextColor,
    onClick
}) => {

    return (
        <button className="{styles.btn}">

        </button>
    )
}

export default Button
